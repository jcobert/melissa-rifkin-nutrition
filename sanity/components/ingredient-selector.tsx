import React, { FC } from 'react'
import { InputProps } from 'sanity'

type Props = {
  //
}

const IngredientSelector: FC<InputProps> = (props) => {
  return <div></div>
}

export default IngredientSelector
